#!/usr/bin/env bash
set -Eeuo pipefail

if ! command -v git-lfs >/dev/null 2>&1; then
  echo "[PRE-PUSH][BŁĄD LFS] Git LFS nie jest zainstalowany."
  exit 30
fi

echo "[PRE-PUSH] Testy Unity są wyłączone. Sprawdzam Git LFS..."
git lfs pre-push "$@"
